{
  "name": "cap-openapi-cds",
  "version": "1.0.0",
  "description": "Generated by cds-services-archetype",
  "license": "ISC",
  "repository": "<Add your repository here>",
  "devDependencies": {
    "@sap/cds-dk": "^7"
  },
  "scripts": {
    "build": "mbt build -t gen --mtar mtar.tar",
    "deploy": "cf deploy gen/mtar.tar",
    "bind": "cds bind -2 cap-openapi-destination,cap-openapi-auth",
    "run:hybrid": "cds bind --exec mvn spring-boot:run"
  }
}
